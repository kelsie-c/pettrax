<body class="menu-page">

<div class="logs-page">
  <header class="header">
    <div class="col-12">
      <nav class="d-flex mx-2 justify-content-between align-items-center">
        <a id="dashboard" href="#"><img src="./img/PetTrax_Logo-Nav.svg" alt="PetTrax logo" width="200px"></a>
        <a id="menu" href="#">Menu</a>
      </nav>
    </div>
  </header>
  <!-- Main horizontal wrapper -->
  <div class="row justify-content-center">
    <!-- Login form wrapper -->
    <div class="col col-10 col-md-6">
      <a id="dashboard" href="#" class="breadcrumb"><< Back to Dashboard</a>
      <h2>Logs</h2><br>

        {{!-- <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                Select a Pet
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                <li><a class="dropdown-item pet-choices" href="#">All</a></li>
                {{#each pets as |pet|}}
                <li><a class="dropdown-item pet-choices" href="#">{{ pet.name }}</a></li>
                {{/each}}
            </ul>
        </div>

        <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton2" data-bs-toggle="dropdown" aria-expanded="false">
                Select an Action
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                <li><a class="dropdown-item event-choices" href="#">All</a></li>
                {{#each events as |event|}}
                <li><a class="dropdown-item event-choices" href="#">{{ pet.events.eventType }}</a></li>
                {{/each}}
            </ul>
        </div>

        <button id="submit" type="submit" value="submit">FILTER</button> --}}

        {{#each pets as |pet|}}
            {{#each events as |event|}}
            <p id="logs"><span style="text-transform: uppercase">{{ pet.name }}</span> had <span style="text-transform: uppercase">{{ event.eventType }}</span> at {{ event.time }} </p>
            {{/each}}
        {{/each}}
    </div>
  </div>

<!-- These links necessary for dropdowns to work! -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.min.js" integrity="sha384-lpyLfhYuitXl2zRZ5Bn2fqnhNAKOAaM/0Kr9laMspuaMiZfGmfwRNFh8HlMy49eQ" crossorigin="anonymous"></script>
  <script src="./js/logs.js"></script>